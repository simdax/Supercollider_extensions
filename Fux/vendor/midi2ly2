#!/bin/sh

try_open () {
				evince $1 || open $1 || xdg-open $1
}

transform () {
				musescore -o $2 $1
}

name=$1 #$(basename $1)
echo transformation $name
transform $name $name.pdf
try_open $name.pdf &
if [ ! -z $2 ]; then
				echo on copie dans $2
				cp $name.pdf $2
				cp $name $2
fi	
