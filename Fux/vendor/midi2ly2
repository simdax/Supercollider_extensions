#/bin/bash

try_open () {
				open $1 || xdg-open $1
}

echo ta race
out=/tmp/tmp.ly
name=$1 #$(basename $1)
echo $out
midi2ly -o $out $1
echo transformation $name
lilypond -o $name $out
echo opening $name
try_open $name.pdf
